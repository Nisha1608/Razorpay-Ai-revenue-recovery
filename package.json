{
  "name": "recover-ai",
  "version": "0.1.0",
  "private": true,
  "description": "AI-assisted payment recovery operations platform",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}

